[project]
name="optimal_control_examples_marimo"
version="0.0.1"
requires-python = ">=3.11"

dependencies = [
  "marimo",
  "numpy",
  "matplotlib",
  "birotor",
  "controllers",
]

[tool.uv.sources]
birotor = { path = "libs/optimal_control_examples/birotor", editable = true }
controllers = { path = "libs/optimal_control_examples/controllers", editable = true }
